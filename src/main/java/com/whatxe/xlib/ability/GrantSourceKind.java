package com.whatxe.xlib.ability;

public enum GrantSourceKind {
    COMMAND,
    MODE,
    UPGRADE_NODE,
    BUNDLE,
    IDENTITY,
    IDENTITY_PROJECTION,
    DELEGATION,
    SUPPORT_PACKAGE,
    ARTIFACT,
    ARTIFACT_UNLOCK,
    PROFILE,
    RECIPE_ADVANCEMENT,
    CUSTOM,
    CUSTOM_MANAGED
}
