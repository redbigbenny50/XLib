package com.whatxe.xlib.cue;

public enum XLibRuntimeCueType {
    ACTIVATION_START,
    ACTIVATION_FAIL,
    CHARGE_PROGRESS,
    RELEASE,
    HIT_CONFIRM,
    INTERRUPT,
    STATE_ENTER,
    STATE_EXIT
}
